import { LucideIcon } from 'lucide-react';

export interface KpiData {
  title: string;
  value: string | number;
  subtitle: string;
  icon: LucideIcon;
  iconBgColor: string;
  iconColor: string;
}
